<section>

    <ul class='breadcrumb'>

        <li><a href='./'>Home</a> <span class='divider'>/</span></li>
        <li class='active'>{{instance.name}}</li>

    </ul>

    <h2>
        Instance — {{instance.name}}
    </h2>

    <table class='table table-hover'>

        <thead>
            <tr>
                <th>Navigate</th>
            </tr>
        </thead>

        <tbody>

            <tr>
                <td class='link'><a href='./#/{{instance.id}}/students'>Students</a></td>
            </tr>

            <tr>
                <td class='link'><a href='./#/{{instance.id}}/courses'>Courses</a></td>
            </tr>

        </tbody>

    </table>

</section>
